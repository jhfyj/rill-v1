/**
 * The RILL wordmark — a static brand mark (no hover animation).
 */
export function Logo({ className }: { className?: string }) {
  return (
    <a href="#" className={className}>
      RILL
    </a>
  );
}

export default Logo;
